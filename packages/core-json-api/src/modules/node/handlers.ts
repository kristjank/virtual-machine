import { IRequest, IResponse } from "../../interfaces";

export async function status(request: IRequest, h: IResponse) {
    // @TODO
}

export async function syncing(request: IRequest, h: IResponse) {
    // @TODO
}

export async function configuration(request: IRequest, h: IResponse) {
    // @TODO
}
